import { Route } from '@angular/router';
import { SubBuComponent } from './sub-bu.component';

export const subBuRoutes: Route[] = [
    {
        path: '',
        component: SubBuComponent
    }
];
