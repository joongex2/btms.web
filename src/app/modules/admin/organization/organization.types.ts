export interface Organization {
    id: number;
    code: string;
    name: string;
    businessCode: string;
    subBusinessCode: string;
    plantCode: string;
    divisionCode: string;
    isActive: boolean;
}